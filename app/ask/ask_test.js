'use strict';

describe('myApp.ask module', function() {

  beforeEach(module('myApp.ask'));

  describe('ask controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var askCtrl = $controller('AskCtrl');
      expect(askCtrl).toBeDefined();
    }));

  });
});